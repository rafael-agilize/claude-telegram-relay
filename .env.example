# ============================================================
# Claude Telegram Relay — Environment Variables
# ============================================================
# Copy to .env:  cp .env.example .env
# Then fill in your values below.
# ============================================================

# --- REQUIRED: Telegram ---

# Bot token from @BotFather
TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather

# Your Telegram user ID (from @userinfobot)
TELEGRAM_USER_ID=your_telegram_user_id

# --- REQUIRED: Supabase (persistent memory) ---

# Project URL (looks like https://abc123.supabase.co)
SUPABASE_URL=your_project_url

# service_role secret key (Settings → API → service_role, starts with eyJ...)
SUPABASE_SERVICE_KEY=your_service_role_key

# --- OPTIONAL: Voice Transcription (Groq Whisper) ---

# Groq API key (console.groq.com)
# GROQ_API_KEY=your_groq_key

# Whisper model (default: whisper-large-v3-turbo)
# GROQ_WHISPER_MODEL=whisper-large-v3-turbo

# Path to ffmpeg for audio conversion (default: /opt/homebrew/bin/ffmpeg)
# FFMPEG_PATH=/opt/homebrew/bin/ffmpeg

# --- OPTIONAL: Voice Responses (ElevenLabs TTS) ---

# ElevenLabs API key (elevenlabs.io)
# ELEVENLABS_API_KEY=your_elevenlabs_key

# ElevenLabs voice ID
# ELEVENLABS_VOICE_ID=your_voice_id

# --- OPTIONAL: Paths ---

# Claude CLI path (auto-detected if in PATH)
# CLAUDE_PATH=claude

# Working directory for Claude (defaults to relay's own directory)
# PROJECT_DIR=/path/to/your/project

# Relay data directory (defaults to ~/.claude-relay)
# RELAY_DIR=~/.claude-relay

# --- OPTIONAL: Telegram Group (for heartbeat topic) ---

# Numeric ID of the supergroup where heartbeat messages go (falls back to DM)
# TELEGRAM_GROUP_ID=your_group_id
